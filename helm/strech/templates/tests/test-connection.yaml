apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "strech.fullname" . }}-test-connection"
  labels:
    {{- include "strech.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "strech.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
