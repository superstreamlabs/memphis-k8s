apiVersion: v1
kind: Service
metadata:
  name: {{ include "strech.fullname" . }}
  labels:
    {{- include "strech.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "strech.selectorLabels" . | nindent 4 }}
